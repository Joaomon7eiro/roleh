import 'package:flutter/material.dart';

class TrackPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Em Breve: Filtro com Música"),
      ),
    );
  }
}
